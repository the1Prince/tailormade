PORT=4000
NODE_ENV=development
# MongoDB Atlas: replace <db_username> and <db_password>. Add /tailormade before ? to use that database.
MONGODB_URI="mongodb+srv://admin:root@cluster0.let9lis.mongodb.net/tailormade?retryWrites=true&w=majority&appName=Cluster0"
JWT_SECRET=your-jwt-secret-min-32-chars
JWT_EXPIRE=7d

# Frontend URLs (CORS, redirects)
ADMIN_WEB_URL=http://localhost:5173
MOBILE_SCHEME=tailormade
FRONTEND_URL=http://localhost:5173
